import React from "react"

const BottomMenu = () => {

    return (
        <div className="bottomMenu">
            <button>Start</button>
            <button>Stop</button>
        </div>
    )
}

export default BottomMenu